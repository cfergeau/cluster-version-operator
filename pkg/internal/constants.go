package internal

const (
	ConfigNamespace    = "openshift-config"
	InstallerConfigMap = "openshift-install"
	ManifestsConfigMap = "openshift-install-manifests"
	SystemNamespace    = "kube-system"
	ClusterConfigMap   = "cluster-config-v1"
)
